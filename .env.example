# Domain สำหรับ HTTPS (Let's Encrypt)
# เปลี่ยนเป็น domain จริง เช่น patrol.example.com
DOMAIN=localhost

# Janus host (ใช้ host network)
# สำหรับ Linux ใช้ host.docker.internal หรือ IP ของ server
JANUS_HOST=host.docker.internal
